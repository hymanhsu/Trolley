#include "flags.h"

//For test
DEFINE_uint32(poolSize, 10, "Pool max size, default is 10");
DEFINE_uint32(timeoutSec, 1, "Timeout seconds, default is 1");
DEFINE_string(remoteip, "127.0.0.1", "Remote ip, default is '127.0.0.1' ");
DEFINE_uint32(remoteport, 18000, "Remote port, default is 18000");
